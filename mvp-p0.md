# سند نیازمندی‌های محصول
## نرم‌افزار مدیریت ساختمان — نسخه MVP / P0

**نسخه:** 1.0  
**وضعیت:** Draft  
**هدف:** تعریف دقیق دامنه، قابلیت‌ها و الزامات نسخه P0 برای توسعه MVP

---

## 1. هدف محصول

هدف محصول، ایجاد یک نرم‌افزار ساده و یکپارچه برای مدیریت روزمره ساختمان است؛ به‌گونه‌ای که مدیر ساختمان بتواند بدون استفاده از Excel یا ابزارهای پراکنده، اطلاعات واحدها و ساکنین، محاسبه و صدور قبض شارژ، دریافت پرداخت‌ها، ثبت هزینه‌ها، مدیریت درخواست‌های تعمیراتی و اطلاع‌رسانی به ساکنین را انجام دهد.

محصول در P0 باید بر سه جریان اصلی تمرکز داشته باشد:

1. **مدیریت مالی و شارژ**
2. **مدیریت عملیات و خرابی‌ها**
3. **ارتباط مدیر با ساکنین**

---

# 2. کاربران سیستم

## 2.1 مدیر ساختمان

مسئول اصلی مدیریت ساختمان.

دسترسی‌ها:
- تعریف و ویرایش ساختمان
- مدیریت واحدها
- مدیریت مالک و ساکن
- تعریف فرمول شارژ
- صدور قبض
- ثبت و مشاهده پرداخت‌ها
- ثبت هزینه‌ها
- مشاهده بدهی‌ها
- مدیریت درخواست‌های تعمیراتی
- ارسال اطلاعیه
- مشاهده گزارش‌های پایه

## 2.2 ساکن

مالک یا مستأجر یک واحد.

دسترسی‌ها:
- مشاهده اطلاعات واحد خود
- مشاهده قبض‌های صادرشده
- مشاهده بدهی
- مشاهده تاریخچه پرداخت
- پرداخت قبض
- ثبت درخواست تعمیراتی
- مشاهده وضعیت درخواست
- مشاهده اطلاعیه‌های ساختمان

## 2.3 مالک غیرساکن

در P0 می‌تواند همان حساب ساکن با نوع کاربری «مالک غیرساکن» باشد و فقط اطلاعات مرتبط با واحد خود را مشاهده کند.

---

# 3. دامنه P0

قابلیت‌های زیر در نسخه P0 الزامی هستند:

### P0-01
مدیریت ساختمان، طبقات و واحدها

### P0-02
مدیریت مالک و ساکن

### P0-03
موتور محاسبه شارژ و صدور قبض

### P0-04
مدیریت پرداخت و بدهی

### P0-05
مدیریت درآمد و هزینه

### P0-06
مدیریت درخواست تعمیرات

### P0-07
اطلاع‌رسانی و اعلان

### P0-08
پنل ساکن

### P0-09
داشبورد مدیر

### P0-10
احراز هویت و کنترل دسترسی

---

# 4. P0-01 — مدیریت ساختمان و واحدها

## 4.1 اطلاعات ساختمان

سیستم باید امکان ثبت این اطلاعات را فراهم کند:

- نام ساختمان
- آدرس
- تعداد بلوک
- تعداد طبقات
- تعداد واحد
- سال ساخت
- اطلاعات تماس مدیر
- شماره تماس اضطراری
- توضیحات

## 4.2 واحد

برای هر واحد:

- شماره واحد
- بلوک
- طبقه
- متراژ
- تعداد پارکینگ
- شماره پارکینگ
- تعداد انباری
- شماره انباری
- تعداد نفرات
- وضعیت واحد
- توضیحات

وضعیت واحد:

- فعال
- خالی
- در حال سکونت
- غیرفعال

## 4.3 الزامات

سیستم باید:
- شماره واحد تکراری در یک ساختمان را نپذیرد.
- امکان ویرایش اطلاعات واحد را فراهم کند.
- تاریخچه تغییرات مهم را حفظ کند.
- امکان جستجو و فیلتر واحدها را فراهم کند.

---

# 5. P0-02 — مدیریت مالک و ساکن

هر واحد می‌تواند داشته باشد:

- یک یا چند مالک
- یک یا چند ساکن
- مستأجر
- مالک غیرساکن

اطلاعات شخص:

- نام و نام خانوادگی
- شماره موبایل
- کد ملی، در صورت نیاز محصول
- نوع رابطه با واحد
- تاریخ شروع سکونت
- تاریخ پایان سکونت
- وضعیت فعال/غیرفعال

## الزامات

سیستم باید:
- ارتباط شخص با واحد را ذخیره کند.
- امکان تغییر ساکن را بدون حذف سابقه قبلی فراهم کند.
- تعداد نفرات ساکن در واحد را قابل تنظیم کند.
- تاریخچه سکونت را حفظ کند.

**نکته مهم:** تعداد نفرات باید یک داده مستقل و قابل استفاده در موتور محاسبه شارژ باشد.

---

# 6. P0-03 — موتور محاسبه شارژ

این بخش هسته اصلی محصول است.

سیستم باید امکان تعریف یک یا چند «آیتم هزینه» برای هر دوره شارژ را فراهم کند.

## 6.1 روش‌های محاسبه

### روش A — مساوی بین واحدها

فرمول:

`کل هزینه / تعداد واحدهای مشمول`

مثال:

کل هزینه = 40,000,000  
تعداد واحد = 20

سهم هر واحد:

`2,000,000`

### روش B — بر اساس تعداد نفرات

فرمول:

`هزینه × تعداد نفرات واحد / مجموع نفرات واحدهای مشمول`

مثال:

کل هزینه = 20,000,000  
مجموع نفرات = 40  
واحد A = 4 نفر

سهم واحد A:

`2,000,000`

### روش C — بر اساس متراژ

فرمول:

`هزینه × متراژ واحد / مجموع متراژ واحدهای مشمول`

### روش D — مبلغ ثابت

برای هر واحد یک مبلغ مشخص.

مثلاً:

`هر واحد = 500,000`

### روش E — هزینه اختصاصی

هزینه فقط برای واحدهای انتخاب‌شده اعمال می‌شود.

مثلاً:

- پارکینگ
- تعمیر داخل یک واحد
- خسارت
- هزینه خاص

### روش F — فرمول ترکیبی

سیستم باید اجازه دهد چند روش در یک قبض ترکیب شوند.

مثال:

`50٪ مساوی + 30٪ متراژ + 20٪ نفرات`

---

# 7. ساختار هزینه در قبض

هر قبض می‌تواند شامل چند ردیف باشد:

| عنوان | روش محاسبه | مبلغ کل |
|---|---|---:|
| شارژ عمومی | مساوی | 20,000,000 |
| آب | نفرات | 10,000,000 |
| هزینه تعمیرات | متراژ | 5,000,000 |
| پارکینگ | اختصاصی | 2,000,000 |

سپس سیستم سهم هر واحد را محاسبه می‌کند.

---

# 8. دوره شارژ

مدیر باید بتواند دوره تعریف کند:

- ماه
- تاریخ شروع
- تاریخ پایان
- تاریخ سررسید
- وضعیت

وضعیت دوره:

- پیش‌نویس
- محاسبه‌شده
- صادرشده
- بسته‌شده

## الزام مهم

بعد از صدور قبض، تغییر داده‌های پایه مثل تعداد نفرات یا متراژ نباید مبلغ قبض قبلی را به‌صورت خودکار تغییر دهد.

برای اصلاح قبض باید عملیات مشخص «بازمحاسبه» یا «اصلاح قبض» وجود داشته باشد.

---

# 9. بدهی و بستانکاری

سیستم باید برای هر واحد این موارد را نگهداری کند:

- بدهی قبلی
- مبلغ قبض جاری
- جریمه
- بستانکاری
- پرداخت
- مانده

فرمول:

`مانده = بدهی قبلی + قبض جاری + جریمه - پرداخت - بستانکاری`

---

# 10. جریمه دیرکرد

در P0 امکان تعریف ساده جریمه الزامی است.

مدیر می‌تواند یکی از این حالت‌ها را انتخاب کند:

- بدون جریمه
- مبلغ ثابت
- درصدی از مبلغ
- مبلغ ثابت به ازای هر روز/دوره

جریمه باید در قبض به‌صورت یک آیتم جداگانه نمایش داده شود.

---

# 11. قبض

هر قبض باید دارای:

- شماره قبض
- واحد
- دوره
- مبلغ پایه
- آیتم‌های هزینه
- بدهی قبلی
- جریمه
- بستانکاری
- مبلغ نهایی
- تاریخ صدور
- تاریخ سررسید
- وضعیت پرداخت

وضعیت:

- پرداخت نشده
- پرداخت ناقص
- پرداخت شده
- منقضی‌شده
- لغوشده

---

# 12. P0-04 — پرداخت و تسویه

سیستم باید امکان ثبت پرداخت را فراهم کند.

در P0 می‌توان دو نوع پرداخت داشت:

### پرداخت آنلاین
از طریق درگاه پرداخت.

### پرداخت دستی
توسط مدیر ثبت می‌شود.

اطلاعات پرداخت:

- مبلغ
- تاریخ
- قبض مرتبط
- روش پرداخت
- شماره پیگیری
- ثبت‌کننده

سیستم باید امکان پرداخت ناقص را نیز پشتیبانی کند.

مثلاً:

قبض = 3,000,000  
پرداخت = 1,500,000  
مانده = 1,500,000

---

# 13. P0-05 — مدیریت هزینه‌های ساختمان

مدیر باید بتواند هزینه جدید ثبت کند.

اطلاعات:

- عنوان
- دسته‌بندی
- مبلغ
- تاریخ
- توضیحات
- پرداخت‌کننده
- فایل/تصویر رسید
- وضعیت تأیید

دسته‌بندی اولیه:

- آب
- برق
- گاز
- آسانسور
- نظافت
- نگهبانی
- تعمیرات
- بیمه
- تجهیزات
- سایر

## گزارش مالی پایه

سیستم باید حداقل این موارد را نمایش دهد:

- درآمد ماه
- هزینه ماه
- خالص درآمد/هزینه
- مجموع بدهی ساکنین
- مجموع پرداخت‌شده
- مجموع هزینه‌ها

---

# 14. P0-06 — درخواست تعمیرات

## ثبت درخواست

ساکن باید بتواند درخواست ثبت کند.

اطلاعات:

- عنوان
- دسته‌بندی
- توضیح
- محل
- عکس
- تاریخ ثبت
- اولویت

دسته‌بندی:

- آسانسور
- تأسیسات
- برق
- آب
- نظافت
- مشاعات
- پارکینگ
- سایر

اولویت:

- عادی
- مهم
- فوری

## وضعیت درخواست

`جدید → در حال بررسی → در حال انجام → انجام شد → بسته شد`

مدیر باید بتواند:

- مسئول درخواست را تعیین کند.
- توضیح اضافه کند.
- هزینه را ثبت کند.
- وضعیت را تغییر دهد.
- درخواست را ببندد.

ساکن باید بتواند وضعیت درخواست خود را مشاهده کند.

---

# 15. P0-07 — اطلاع‌رسانی

مدیر باید بتواند اطلاعیه منتشر کند.

اطلاعات:

- عنوان
- متن
- تاریخ انتشار
- تاریخ انقضا
- مخاطب
- فایل پیوست

مخاطب:

- همه ساختمان
- بلوک
- طبقه
- واحد مشخص

اعلان باید حداقل در داخل سیستم نمایش داده شود.

در صورت وجود زیرساخت، Push Notification نیز در P0 قابل پیاده‌سازی است.

---

# 16. P0-08 — پنل ساکن

صفحه اصلی ساکن باید این اطلاعات را نشان دهد:

### وضعیت مالی

- مبلغ قابل پرداخت
- آخرین قبض
- سررسید
- وضعیت پرداخت

### درخواست‌ها

- تعداد درخواست‌های باز
- آخرین درخواست
- وضعیت آن

### اطلاعیه‌ها

- آخرین اطلاعیه‌ها
- تعداد اطلاعیه‌های خوانده‌نشده

## منوی اصلی

- خانه
- شارژها
- پرداخت‌ها
- تعمیرات
- اطلاعیه‌ها
- پروفایل

---

# 17. P0-09 — داشبورد مدیر

داشبورد باید در یک نگاه وضعیت ساختمان را مشخص کند.

### کارت‌های اصلی

- تعداد واحدها
- واحدهای بدهکار
- مبلغ کل بدهی
- درآمد ماه
- هزینه ماه
- درخواست‌های باز

### اقدامات سریع

- صدور شارژ
- ثبت هزینه
- ثبت پرداخت
- ثبت درخواست
- ارسال اطلاعیه

### هشدارها

مثلاً:

- ۷ واحد بدهکار هستند.
- قبض ۵ واحد سررسید گذشته است.
- ۳ درخواست تعمیر باز هستند.
- یک هزینه در انتظار ثبت/تأیید است.

---

# 18. P0-10 — احراز هویت و دسترسی

روش ورود پیشنهادی:

**شماره موبایل + OTP**

پس از ورود، کاربر به ساختمان و واحد مرتبط خود دسترسی خواهد داشت.

## الزامات امنیتی

- ساکن فقط داده‌های واحد خود را ببیند.
- مدیر فقط ساختمان‌های مجاز خود را ببیند.
- داده مالی ساکنین دیگر برای ساکن قابل مشاهده نباشد.
- عملیات حساس دارای ثبت زمان و کاربر انجام‌دهنده باشد.

---

# 19. Notifications / اعلان‌ها

رخدادهای مهم باید قابلیت ایجاد اعلان داشته باشند:

- صدور قبض
- نزدیک‌شدن سررسید
- دیرکرد پرداخت
- ثبت پرداخت
- ثبت درخواست تعمیر
- تغییر وضعیت تعمیر
- انتشار اطلاعیه

---

# 20. Audit Log

سیستم باید عملیات مهم را ثبت کند:

- ایجاد/ویرایش واحد
- تغییر ساکن
- تغییر فرمول شارژ
- صدور قبض
- لغو قبض
- ثبت پرداخت
- ثبت هزینه
- تغییر وضعیت تعمیرات

اطلاعات ثبت‌شده:

- کاربر
- عملیات
- زمان
- شیء مربوطه
- مقدار قبل/بعد، در عملیات حساس

---

# 21. نیازمندی‌های غیرعملکردی

## عملکرد

در حالت عادی، صفحات اصلی باید با تأخیر قابل قبول بارگذاری شوند.

محاسبه قبض برای یک ساختمان معمولی باید در چند ثانیه انجام شود.

## قابلیت اطمینان

هیچ قبض صادرشده نباید به‌دلیل تغییر اطلاعات واحد، به‌صورت ناخواسته تغییر کند.

## امنیت

اطلاعات مالی و شخصی باید فقط بر اساس سطح دسترسی در دسترس باشد.

## ثبت سوابق

اطلاعات مالی و تراکنش‌ها نباید با حذف ساده کاربر یا واحد از بین بروند.

---

# 22. جریان اصلی مدیر

```text
ورود مدیر
   ↓
انتخاب ساختمان
   ↓
تعریف واحدها
   ↓
تعریف مالک/ساکن
   ↓
تعریف فرمول شارژ
   ↓
تعریف دوره
   ↓
محاسبه قبض
   ↓
بازبینی
   ↓
صدور قبض
   ↓
اعلان به ساکنین
   ↓
پرداخت
   ↓
ثبت هزینه‌ها
   ↓
گزارش مالی
```

---

# 23. جریان اصلی ساکن

```text
ورود
 ↓
مشاهده قبض
 ↓
مشاهده جزئیات
 ↓
پرداخت
 ↓
مشاهده رسید

یا:

ورود
 ↓
ثبت خرابی
 ↓
پیگیری وضعیت
 ↓
دریافت اعلان انجام کار
```

---

# 24. قوانین کسب‌وکار مهم

### BR-01
هر واحد فقط یک وضعیت مالی جاری دارد، اما می‌تواند تعداد نامحدودی قبض تاریخی داشته باشد.

### BR-02
پرداخت می‌تواند بخشی از قبض را تسویه کند.

### BR-03
قبض صادرشده نباید بدون ثبت عملیات اصلاحی تغییر کند.

### BR-04
تعداد نفرات باید قابل تغییر باشد و تاریخچه آن حفظ شود.

### BR-05
تغییر تعداد نفرات نباید قبض‌های قبلی را تغییر دهد.

### BR-06
واحد خالی می‌تواند در بعضی انواع هزینه مشمول یا غیرمشمول باشد.

### BR-07
هر آیتم هزینه باید مشخص کند چه واحدهایی مشمول آن هستند.

### BR-08
فرمول شارژ باید قابل مشاهده و قابل بررسی قبل از صدور قبض باشد.

### BR-09
جمع مبالغ سهم واحدها باید با مبلغ هزینه قابل تسویه باشد؛ سیستم باید اختلاف ناشی از گرد کردن را مدیریت کند.

### BR-10
لغو قبض باید سابقه قبض را حذف نکند.

---

# 25. سناریوی نمونه محاسبه

فرض:

۱۰ واحد

هزینه عمومی: 10,000,000 تومان  
روش: مساوی

هزینه آب: 8,000,000 تومان  
روش: تعداد نفرات

تعداد کل نفرات: 20

واحد ۱: ۴ نفر

پس:

### هزینه عمومی
`10,000,000 ÷ 10 = 1,000,000`

### سهم آب واحد ۱
`8,000,000 × 4 ÷ 20 = 1,600,000`

### قبض واحد ۱

`1,000,000 + 1,600,000 = 2,600,000`

در صورت وجود بدهی قبلی، جریمه یا بستانکاری، آن‌ها نیز در انتهای محاسبه اعمال می‌شوند.

---

# 26. معیار پذیرش MVP

نسخه P0 زمانی قابل پذیرش است که مدیر بتواند بدون دخالت تیم فنی:

1. ساختمان و واحدها را تعریف کند.
2. مالک و ساکن تعریف کند.
3. تعداد نفرات هر واحد را ثبت کند.
4. فرمول شارژ را تعریف کند.
5. یک دوره شارژ ایجاد کند.
6. قبض واحدها را محاسبه کند.
7. نتیجه محاسبه را قبل از صدور مشاهده کند.
8. قبض‌ها را صادر کند.
9. پرداخت ثبت کند.
10. بدهی هر واحد را مشاهده کند.
11. هزینه‌های ساختمان را ثبت کند.
12. درخواست تعمیر دریافت و مدیریت کند.
13. اطلاعیه منتشر کند.
14. ساکن بتواند قبض، پرداخت، تعمیرات و اطلاعیه‌های خود را ببیند.

---

# 27. موارد خارج از P0

این موارد در MVP پیاده‌سازی نمی‌شوند:

- مدیریت پارکینگ پیشرفته
- رزرو مشاعات
- مدیریت قرارداد پیمانکار
- حسابداری دوطرفه کامل
- انبارداری
- کنترل تردد
- تشخیص پلاک
- IoT
- AI
- مدیریت چندلایه هیئت‌مدیره
- اپ Native مستقل
- سیستم CRM
- گزارش‌های مالی پیشرفته

---

# 28. پیشنهاد معماری محصول در سطح مفهومی

محصول از چهار هسته اصلی تشکیل شود:

```text
Identity
   ├── Users
   ├── Roles
   └── Access

Building
   ├── Buildings
   ├── Units
   ├── Residents
   └── Occupancy

Finance
   ├── Charge Rules
   ├── Billing Periods
   ├── Invoices
   ├── Payments
   ├── Expenses
   └── Balances

Operations
   ├── Maintenance Requests
   ├── Announcements
   └── Notifications
```

---

# 29. تصمیم محصول

**هسته P0 نباید «مدیریت ساختمان» به معنای گسترده باشد؛ باید «مدیریت شارژ و عملیات روزانه ساختمان» باشد.**

سه خروجی اصلی که باید در پایان P0 به‌درستی کار کنند:

**۱. مدیر بتواند قبض را درست محاسبه و وصول کند.**

**۲. مدیر بتواند هزینه و خرابی ساختمان را مدیریت کند.**

**۳. ساکن بتواند وضعیت مالی و درخواست‌های خود را بدون تماس با مدیر پیگیری کند.**

هر Feature دیگری که مستقیماً به این سه خروجی کمک نمی‌کند، بهتر است در P0 وارد نشود.